## This code calles GraphComponent which is the react component into th streamlit code.

The component code can be found at template/my_component/frontend/src/GraphComponent.

The Streamlit file is located at template/my_component/__init__.py.

Please look at line number 75 in the __init__.py file to see the graph_component call.

The data returned from the React component is stored in the graph_data variable, which we can use.
